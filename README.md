# n8n-Weather-Alert-Automation-API-Conditions-Email-
A weather-alert workflow using an HTTP API call to fetch temperature data, apply conditional logic, and send an alert email if thresholds are exceeded.
